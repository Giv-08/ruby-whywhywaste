<%= link_to '🡰', 'javascript:history.back()' %>
<div class="container">
  <p><strong>checkout</strong></p>

  <p> Total price:  <%= @order.calculate_total_price %></p>

  <p>Payment Options:</p>
  <button>Cash</button>
  <% if @order.status == "cart" %>
    <%= button_to "Checkout Order", checkout_order_path(@order), method: :patch, data: { confirm: "Are you sure you want to checkout this order?" }, class: "btn btn-primary" %>
  <% end %>
</div>
