<div class="nav-top">
<%# go-back-icon %>
<div class="back"><%= link_to 'javascript:history.back()' do %>
<i class="fa-solid fa-angle-left"></i>
<% end %></div>
<%= image_tag "www-logo.png", alt: "Logo", class: "logo-img" %>
<%#= button_tag "Search", type: :button, onclick: "window.location.href = '#'", class: "button-negative" %>
</div>
<%# dont need anymore %>
<div class="search">
<div class="back"><%= link_to 'javascript:history.back()' do %>
<i class="fa-solid fa-angles-left" style="color: #F3844C;"></i>
<% end %></div>
</div>
<h2 class="text-center">Welcome to <%= current_user.first_name %> <%= current_user.last_name %>'s notifications</h2>

<%# take infos from orders table %>
<% current_user.orders.each do |order| %>
  <div class="notification-card mt-3 ">
    <div class="notfication-text">
      <p>Restaurant: <%= order.restaurant_id %></p>
      <p>Order id: <%= order.id %></p>
      <p>Customer: <%= order.user.first_name%> <%= order.user.last_name%>
      <p>Total price: <%= order.calculate_total_price %></p>
    </div>
  </div>
  <% end %></p>
  <br><br>
